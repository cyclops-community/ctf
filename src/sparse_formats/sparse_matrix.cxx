#include "sparse_matrix.h"
#include "../shared/util.h"

namespace CTF_int {
  int64_t sparse_matrix::size() const {
    ASSERT(0);
    assert(0);
  }

  void sparse_matrix::partition(int s, char ** parts_buffer, sparse_matrix ** parts){
    ASSERT(0);
    assert(0);
  }
      /**
       * \brief constructor merges parts into one CSR matrix, assuming they are split by partition() (Above)
       */
  void sparse_matrix::assemble(char * const * smnds, int s){
    ASSERT(0);
    assert(0);
  }
}

